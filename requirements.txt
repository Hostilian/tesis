# Root-level requirements.txt
# This file is for legacy admin tools (chatai.py, engine/) preserved in the repo root.
# For the main satellite data pipeline, use:
#   pip install -r pipeline/requirements.txt
# For development tools (pytest, black, flake8):
#   pip install -r pipeline/requirements-dev.txt

# Legacy terminal chatbot dependencies (kept for archival purposes)
rich>=13.0.0
prompt_toolkit>=3.0.0
requests>=2.31.0
toml>=0.10.2
